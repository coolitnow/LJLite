<h1 style="color:purple; font-family:courier;">Entries</h1>

<ul>
    <% @entries.each do |entry| %>
    <li>
        <a href="/entries/<%= entry.id%>"> 
        <%= entry.title %>
        </a>
    </li>
    <% end %>
</ul>